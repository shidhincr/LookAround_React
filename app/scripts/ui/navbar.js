var React = require('react');

/** @jsx React.DOM */
var Navbar = React.createClass({
    displayName: 'Navbar',
    render: function () {
        return (
            <div>Navbar</div>
        );
    }
});

module.exports = Navbar