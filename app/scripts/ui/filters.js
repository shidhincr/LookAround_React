/** @jsx React.DOM */
var React = require('react');

var Filters = React.createClass({
    displayName: 'Filters',
    render: function () {
        return (
            <div>Filters</div>
        );
    }
});

module.exports = Filters;