/** @jsx React.DOM */
var React = require('react');

var Results = React.createClass({
    displayName: 'Results',
    render: function () {
        return (
            <div>Results</div>
        );
    }
});

module.exports = Results;